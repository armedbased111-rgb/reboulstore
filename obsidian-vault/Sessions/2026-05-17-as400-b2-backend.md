---
type: session
date: 2026-05-17
sujet: AS400 — B2 module NestJS sync-as400
statut: en-cours
---
# Session — AS400 B2 backend

## Décision architecture

- **Prod** : module NestJS `sync-as400` sur VPS (`@Cron`, écriture `sortant/`)
- **CLI** : tests manuels B1 seulement (`export-csv` + `sftp put`)

## Prochaine étape (toi)

1. Créer `backend/src/modules/sync-as400/` (module, service, scheduler)
2. Enregistrer dans `app.module.ts`
3. `exportSortant()` : produits `is_published` + variants → CSV `;`
4. Config `AS400_SORTANT_DIR`

Réf : [[Securite/as400#3b — Implémentation]] · modèle [[Backend/images-optimization]]
