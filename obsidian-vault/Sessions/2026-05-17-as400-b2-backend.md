---
type: session
date: 2026-05-18
sujet: AS400 B2 — module NestJS sync-as400
statut: terminé
---
# Session — AS400 B2 backend (18/05/2026)

## Livré

- Module `backend/src/modules/sync-as400/` — export catalogue publié → CSV `;`
- `POST /sync-as400/export` (header `X-As400-Export-Secret`)
- Prod : `produits_reboul.csv` dans `/var/sftp/as400/sortant/` — **1460 lignes** ✅
- Docker prod : bind mount `sortant/` ; secret dans `.env.production` uniquement
- Script VPS : `scripts/setup-as400-sortant-acl.sh` (package `acl` installé)

## B3 (18/05)

- Snapshot `.as400-export-state.json` · delta `change_type` · resync complet 7 j
- `POST /sync-as400/export?full=true` pour forcer un complet

## B4 (18/05)

- `sync-as400.scheduler.ts` — cron `0 * * * *`, `AS400_EXPORT_CRON_ENABLED=true` en prod

## Clôture session 18/05

→ [[Sessions/2026-05-18-as400-phase3-complete]] — Phase 3 sortant terminée côté Reboul
