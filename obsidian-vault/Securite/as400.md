---
type: securite
node: as400
maj: 2026-05-17
---
# AS400 — Intégration SFTP ↔ Reboul Store

Liens : [[Securite/Securite]] · [[Securite/as400-fiche-expert]] · [[Sessions/2026-05-17-as400-phase1-setup]] · `/as400-integration`

| Fichier | Rôle |
|---------|------|
| **Ce fichier** | Plan + **tâches par phase** (lire dans l’ordre) |
| [[Securite/as400-fiche-expert]] | Document à envoyer à l’expert (plus tard) |

**Réunion 12/05/2026** ✅ — SFTP bidirectionnel, CSV, batch horaire.

---

## Modèle (1 minute)

- Pas de commande par commande (`orders`) : **extraits produits / stocks** comme la **feuille de stock** actuelle.
- **Symétrie** : AS400 dépose dans `entrant/` → on met à jour le site ; nous déposons dans `sortant/` → ils intègrent chez eux.
- **Phase 3** = deux morceaux : **3a cadrage** (décisions) ✅ · **3b code** (générer + déposer le CSV) ⏳

---

## Vue d’ensemble

| Phase | Dossier | Objectif | Statut |
|-------|---------|----------|--------|
| **1** | — | Accès SFTP | ✅ VPS · [ ] envoi fiche expert |
| **2** | `entrant/` | Leur CSV → maj DB Reboul | ⏸️ attend 1er CSV |
| **3** | `sortant/` | Notre CSV (delta) → AS400 | ✅ 3a · ⏳ **3b** |

---

## Phase 1 — SFTP

**But** : ouvrir le tiroir (`entrant/` + `sortant/`).  
**Guide install** : [[#Annexe — Setup SFTP (VPS, déjà fait)]]

### Tâches

- [x] User `sftp-as400`, chroot, dossiers `entrant/` + `sortant/`
- [x] Test upload SFTP (`entrant/`)
- [ ] Transmettre [[Securite/as400-fiche-expert]] + mot de passe (séparé, plus tard)

### Identifiants (référence)

| Paramètre | Valeur |
|-----------|--------|
| Host | `152.228.218.35` |
| Port | `22` |
| User | `sftp-as400` |
| Mot de passe | Gestionnaire de mots de passe — **jamais** dans git |

---

## Phase 2 — Entrant (AS400 → Reboul)

**But** : chaque heure, **leur** CSV remplace la feuille de stock + import manuel → upsert stocks en BDD.  
**Bloqué par** : 1er fichier réel ou spec colonnes AS400.

### Tâches

- [x] Granularité actée : 1 ligne = 1 ligne feuille (réf + stock)
- [ ] Recevoir 1er CSV dans `entrant/` ou spec colonnes
- [ ] Valider mapping colonnes (Marque / Genre / Référence / Stock ?)
- [ ] *(code)* Parser + brancher sur logique `reboul-import` (upsert)
- [ ] *(code)* Cron horaire + archivage fichiers
- [ ] *(code)* Alerte si pas de fichier depuis 2 h
- [ ] *(code)* Réconciliation stock hebdo AS400 ↔ DB

### Rappel process actuel (hors SFTP)

`feuille stock` → `./rcli import feuille-to-csv` → Admin import → même logique à automatiser.

---

## Phase 3 — Sortant (Reboul → AS400)

**But** : chaque heure, **notre** CSV (lignes **modifiées**) dans `sortant/` pour l’AS400.

### 3a — Cadrage métier ✅ (17/05)

Décisions Reboul — **fait** :

| Sujet | Décision |
|-------|----------|
| Contenu | Extrait **produits** (feuille / import), **pas** `orders` |
| Granularité | Comme feuille de stock (ref + stock, schéma BDD OK) |
| Champs | Tous champs produit + variant — affinage plus tard |
| Périmètre | Catalogue **actif** complet |
| Fichier | **Delta** (lignes modifiées) ; 1er envoi **complet** ; resync hebdo complète |
| Stock | Un seul stock = **stock web** (`variants`) |

Détail / colonnes / delta : [[#Annexe — Spec CSV (référence)]]

### 3b — Implémentation ⏳ (la suite — **peut démarrer sans Phase 2**)

**But** : programmer génération CSV + dépôt dans `sortant/`.

### Tâches 3b

- [x] **B1** Export CSV test (`./rcli db export-csv`) + `put` → `sortant/produits_reboul_test.csv` ✅ 17/05
- [x] **B1** Fichier visible sur VPS (`/var/sftp/as400/sortant/`) ✅
- [ ] *(recommandé)* Expert valide : flux produits + import **delta**
- [ ] **B2** Module / script : export catalogue actif → écriture `sortant/` (sans SFTP manuel)
- [ ] **B3** Delta : comparer au dernier export + lignes `delete`
- [ ] **B3** 1er run complet automatisé ; puis delta ; resync hebdo complète
- [ ] **B4** Cron horaire
- [ ] Expert confirme qu’il peut **importer** le CSV côté AS400
- [ ] *(optionnel)* Aligner colonnes sur 1er CSV `entrant/`

---

## Questions pour l’expert (à l’envoi de la fiche)

- [ ] IP fixe AS400 → UFW
- [ ] Colonnes exactes du CSV `entrant/`
- [ ] `sortant/` = bien extrait **produits** en **delta** (pas ventes commande par commande) ?
- [ ] « Mouvements » réunion 12/05 = ce flux ou autre chose ?

---

## Risques

| Risque | Mitigation |
|--------|-----------|
| CSV malformé | Validation + rollback + alerte |
| Plus de fichier entrant | Alerte après 2 h |
| Accès SFTP abusif | Chroot + IP whitelist + fail2ban |
| Désync stock | Réconciliation hebdo |

---

## Annexe — Setup SFTP (VPS, déjà fait)

Session : [[Sessions/2026-05-17-as400-phase1-setup]]

```bash
sudo groupadd sftp-as400
sudo mkdir -p /var/sftp/as400/{entrant,sortant}
sudo useradd -g sftp-as400 -d /var/sftp/as400 -s /usr/sbin/nologin -M sftp-as400
sudo chown root:root /var/sftp/as400 && sudo chmod 755 /var/sftp/as400
sudo chown sftp-as400:sftp-as400 /var/sftp/as400/entrant /var/sftp/as400/sortant
```

Fichier `/etc/ssh/sshd_config.d/sftp-as400.conf` :

```
Match User sftp-as400
    ChrootDirectory /var/sftp/as400
    ForceCommand internal-sftp
    AllowTcpForwarding no
    X11Forwarding no
    PasswordAuthentication yes
    KbdInteractiveAuthentication yes
    AuthenticationMethods password
```

```bash
sudo sshd -t && sudo systemctl reload ssh
sudo passwd sftp-as400
```

Test Mac :

```bash
sftp -o PreferredAuthentications=password -o PubkeyAuthentication=no -P 22 sftp-as400@152.228.218.35
```

---

## Annexe — Spec CSV (référence)

### Pourquoi pas `orders` ?

L’AS400 intègre des **fichiers de données** en batch, pas une commande web à ressaisir.

### Colonnes (hypothèse = feuille de stock)

| Feuille / AS400 | Reboul BDD |
|-----------------|------------|
| Marque | `brand` |
| Genre | `category` |
| Référence | `reference` (+ size/sku dérivés) |
| Stock | `variant.stock` |

Import complet déjà supporté : `name;reference;brand;category;collection;stock;price` (+ `color;size;sku`).  
Code : `admin-central/.../reboul-import.service.ts` · ex. `docs/imports/import-stone-island-ss26.csv`

### Delta (sortant)

- Chaque ligne du fichier = une **modification** (pas de couleur possible en CSV pur).
- Pistes code : table « dernier export », colonne `change_type` (`update` | `delete`), complet hebdomadaire.
