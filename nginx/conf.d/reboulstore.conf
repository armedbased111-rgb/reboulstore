# Configuration Reboul Store - Reverse Proxy & Frontend

# Redirection HTTP → HTTPS (pour www.reboulstore.com)
server {
    listen 80;
    server_name www.reboulstore.com;
    return 301 https://$server_name$request_uri;
}

# Redirection HTTP → HTTPS (pour admin.reboulstore.com)
server {
    listen 80;
    server_name admin.reboulstore.com;
    return 301 https://$server_name$request_uri;
}

# Configuration Admin Central - Proxy vers admin-central-nginx (HTTPS)
# Résolution au runtime (resolver) pour éviter "host not found" au démarrage
server {
    listen 443 ssl;
    http2 on;
    server_name admin.reboulstore.com;

    # SSL Configuration (même certificat que www)
    ssl_certificate /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Cloudflare only — bloquer accès direct IP (#9)
    if ($cloudflare_ip = 0) {
        return 444;
    }

    resolver 127.0.0.11 valid=10s;
    set $admin_backend "http://admin-central-nginx-prod:80";

    location / {
        proxy_pass $admin_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}

# Redirection reboulstore.com → www.reboulstore.com (HTTP)
server {
    listen 80;
    server_name reboulstore.com;
    return 301 http://www.reboulstore.com$request_uri;
}

# Redirection reboulstore.com → www.reboulstore.com (HTTPS)
server {
    listen 443 ssl;
    http2 on;
    server_name reboulstore.com;

    # SSL Configuration
    ssl_certificate /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    return 301 https://www.reboulstore.com$request_uri;
}

# Configuration API Backend - api.reboulstore.com (HTTPS)
server {
    listen 443 ssl;
    http2 on;
    server_name api.reboulstore.com;

    ssl_certificate /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Cloudflare only (#9)
    if ($cloudflare_ip = 0) {
        return 444;
    }

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    limit_req zone=api_limit burst=20 nodelay;

    resolver 127.0.0.11 valid=10s;
    set $backend_upstream "http://backend:3001";

    location / {
        proxy_pass $backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}

# Redirection HTTP → HTTPS pour api.reboulstore.com
server {
    listen 80;
    server_name api.reboulstore.com;
    return 301 https://$server_name$request_uri;
}

# Configuration principale Reboul Store HTTPS
server {
    listen 443 ssl;
    http2 on;
    server_name www.reboulstore.com;

    ssl_certificate /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Cloudflare only (#9)
    if ($cloudflare_ip = 0) {
        return 444;
    }

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    resolver 127.0.0.11 valid=10s;
    set $backend_upstream "http://backend:3001";

    root /usr/share/nginx/html;
    index index.html;

    # Cache pour assets statiques (images, fonts, etc.)
    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp|avif)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Cache pour fichiers CSS et JS (avec versioning dans le nom)
    location ~* \.(css|js)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Cache pour fonts
    location ~* \.(woff|woff2|ttf|eot|otf)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Rate limiting zones (définies dans nginx.prod.conf)

    # API Backend - Reverse Proxy (avec rate limiting)
    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;
        rewrite ^/api(.*)$ $1 break;
        proxy_pass $backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }

    # API Auth - Rate limiting plus strict
    location /api/auth/ {
        limit_req zone=auth_limit burst=10 nodelay;
        rewrite ^/api(.*)$ $1 break;
        proxy_pass $backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }

    # Health check endpoint
    location /health {
        proxy_pass $backend_upstream/health;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        access_log off;
    }

    # Product pages: no CDN mixing HTML variants; bots get OG HTML from backend (single og:image = product photo)
    location ~ ^/product/(\d+) {
        add_header Cache-Control "private, no-cache, must-revalidate" always;
        add_header Vary "User-Agent" always;

        set $is_bot 0;
        if ($http_user_agent ~* "(facebookexternalhit|Facebot|Twitterbot|WhatsApp|TelegramBot|LinkedInBot|Slackbot|Discordbot|Applebot|iMessageBot|Googlebot|bingbot|Baiduspider|Pinterestbot|Embedly|Quora Link Preview|Showyoubot|outbrain|vkShare|W3C_Validator|redditbot|Slurp|Sogou|Bytespider|Amazonbot|GPTBot|ChatGPT-User|Claude-Web|PerplexityBot)") {
            set $is_bot 1;
        }
        if ($is_bot = 1) {
            rewrite ^/product/(\d+)$ /og/product/$1 break;
            proxy_pass $backend_upstream;
        }
        try_files $uri /index.html;
    }

    # Frontend React Router - Toutes les routes non-API servent index.html
    # MAINTENANCE MODE : commenter le "return 503" pour passer en live
    location / {
        return 503;
        # try_files $uri $uri/ /index.html;
        # expires -1;
        # add_header Cache-Control "no-cache, no-store, must-revalidate";
        # add_header Pragma "no-cache";
    }

    # Fichiers statiques du frontend (assets avec hash dans le nom)
    location /assets {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Error pages
    error_page 404 /index.html;
    error_page 500 502 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }

    # MAINTENANCE MODE — retirer le return 503 ci-dessous pour passer en live
    error_page 503 /maintenance.html;
    location = /maintenance.html {
        alias /etc/nginx/maintenance.html;
        internal;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    }
}
